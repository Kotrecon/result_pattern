# 🧪 Result — Типобезопасная обработка результатов и ошибок в .NET

[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)
[![GitHub last commit](https://img.shields.io/github/last-commit/Kotrecon/result_pattern)](https://github.com/Kotrecon/result_pattern)

> **«Мой подход к валидации: без атрибутов, без исключений для бизнес-ошибок — только явные, тестируемые и безопасные результаты»**

Этот фреймворк позволяет заменить `try/catch` и `[Required]` на **чистую, функциональную модель результата операции**, где успех и ошибка — это **первоклассные граждане кода**, а не побочные эффекты.

---

## 💡 Зачем это нужно?

Вместо:

```csharp
[Required] public string Email { get; set; } // ← валидация "где-то там"
// или
if (string.IsNullOrEmpty(email)) throw new ArgumentException("..."); // ← исключение как поток управления
```

Вы пишете:

```csharp
var result = CreateUser(email, password);
if (!result.IsSuccess)
{
    // Обрабатываем конкретную ошибку: ValidationError, NotFoundError и т.д.
}
```

✅ Преимущества:

- **Нет исключений** для ожидаемых ошибок (валидация, доступ, конфликты)
- **Типобезопасность**: компилятор не даст забыть обработать ошибку
- **Читаемость**: сигнатура метода говорит всё (`Result<User>` → может завершиться неудачей)
- **Тестируемость**: проверяете тип ошибки, а не текст сообщения
- **Интеграция с ASP.NET Core**: один вызов → стандартный `ProblemDetails`

---

## 📦 Основные компоненты

### 1. `Result<T>` и `Result`

- `Result<T>.Success(value)` — успешный результат с данными
- `Result<T>.Failure(error)` — неуспешный результат с ошибкой(ами)
- Поддержка цепочек через `.Map()` и `.Bind()`

### 2. `IError` — интерфейс ошибки

Любая ошибка должна реализовывать:

```csharp
public interface IError
{
    string Message { get; }   // Для логов и UI
    string Code { get; }      // Для клиентов и switch
    int StatusCode { get; }   // Для HTTP
}
```

### 3. Готовые типы ошибок

| Тип                 | Код                     | HTTP | Использование               |
| ------------------- | ----------------------- | ---- | --------------------------- |
| `ValidationError`   | `ValidationFailed`      | 422  | Некорректные входные данные |
| `NotFoundError`     | `NotFound`              | 404  | Сущность не найдена         |
| `ForbiddenError`    | `Forbidden`             | 403  | Нет прав                    |
| `ConflictError`     | `Conflict`              | 409  | Конфликт (дубликат email)   |
| `BusinessRuleError` | `BusinessRuleViolation` | 400  | Нарушение бизнес-логики     |

### 4. Интеграция с ASP.NET Core

```csharp
// В контроллере или minimal API
return userService.CreateUser(dto).ToActionResult();
```

→ Автоматически возвращает:

- `200 OK` при успехе
- `422 Unprocessable Entity` с деталями валидации при ошибке

Для `ValidationError` ответ включает поле `errors`:

```json
{
  "type": "https://tools.ietf.org/html/rfc7807",
  "title": "ValidationFailed",
  "status": 422,
  "detail": "Email не может быть пустым",
  "errors": ["Поле Email обязательно"]
}
```

---

## 🛠 Примеры использования

### Базовая валидация

```csharp
static Result<User> CreateUser(string email, string name)
{
    var errors = new List<IError>();

    if (string.IsNullOrWhiteSpace(email))
        errors.Add(new ValidationError(
            "Email не может быть пустым",
            new[] { "Поле Email обязательно" }
        ));

    if (errors.Any())
        return Result<User>.Failure(errors);

    return Result<User>.Success(new User(email, name));
}
```

### Цепочка операций (без вложенных if!)

```csharp
GetUserById(123)
    .Bind(user => user.IsActive
        ? Result<User>.Success(user)
        : Result<User>.Failure(new ForbiddenError()))
    .OnSuccess(u => Console.WriteLine($"Привет, {u.Name}!"));
```

### В контроллере

```csharp
[HttpPost]
public IActionResult Register(RegisterDto dto)
{
    var result = _userService.Register(dto.Email, dto.Password);
    return result.ToActionResult(); // ← всё!
}
```

---

## 🚀 Дальнейшее развитие (Roadmap)

Этот фреймворк — основа для расширяемой системы обработки ошибок. Планируемые улучшения:

- ✅ **Поддержка детализированных ошибок валидации по полям**  
  → `{"email": ["неверный формат"], "password": ["слишком короткий"]}`

- 🔜 **Интеграция с MediatR**  
  → Реализация `IRequestHandler<T, Result<R>>` для CQRS

- 🔜 **Поддержка Minimal APIs**  
  → Кастомные `Results` через `Results.Extensions`

- 🔜 **Middleware для логирования ошибок**  
  → Автоматическая запись `IError` в Serilog / Application Insights

- 🔜 **OpenAPI-документация**  
  → Генерация схем `ProblemDetails` в Swagger(Scalar) (через Swashbuckle)

- 🔜 **Локализация по `Code`**  
  → Замена `Message` на локализованную строку на основе `Code` и `Culture`

- 🔜 **Превращение в NuGet-библиотеку**  
  → Публикация как `YourNamespace.Result` для переиспользования в других проектах

---

## 📝 Как заменить атрибуты валидации?

Вместо:

```csharp
public class RegisterDto
{
    [Required, EmailAddress]
    public string Email { get; set; }
}
```

Создайте **валидатор как отдельный сервис**:

```csharp
public class RegisterValidator
{
    public Result Validate(RegisterDto dto)
    {
        var errors = new List<IError>();
        if (string.IsNullOrEmpty(dto.Email))
            errors.Add(new ValidationError("...", new[] {"Email обязателен"}));
        // ...
        return errors.Any() ? Result.Failure(errors) : Result.Success();
    }
}
```

→ Это даёт **полный контроль**, **тестирование** и **единый стиль** по всему приложению.

---

## 🧾 Шпаргалка (Cheat Sheet)

| Операция       | Код                                                |
| -------------- | -------------------------------------------------- |
| Успех          | `Result<T>.Success(value)`                         |
| Ошибка         | `Result<T>.Failure(new NotFoundError("User", id))` |
| Цепочка        | `.Bind(x => NextOperation(x))`                     |
| Преобразование | `.Map(x => x.Name)`                                |
| В контроллере  | `return result.ToActionResult();`                  |
| Обработка      | `result.OnSuccess(...).OnFailure(...)`             |

---

## 🎯 Почему это «мой» подход?

- Это **не FluentValidation** — но он **проще, легче и полностью под вашим контролем**.
- Он **не прячет логику** в атрибутах или магических аннотациях.
- Он **заставляет думать о возможных ошибках** на этапе проектирования.
- Он **масштабируется**: от простого консольного приложения до распределённой системы.

> «Хороший код не скрывает ошибки — он делает их частью контракта».

---

## 📥 Установка

Скопируйте папку `Common/` в свой проект.  
Добавьте `using YourNamespace.Common;` в нужные файлы.

_(В будущем — через NuGet: `dotnet add package YourNamespace.Result`)_

---

## 🙌 Вклад

PR и идеи приветствуются! Особенно:

- Поддержка нескольких ошибок валидации по полям
- Примеры с MediatR и Minimal APIs
- Локализация через `IStringLocalizer`

---

## 👨‍💻 Автор

**`@Kotrecon`**

Архитектор решений из Санкт-Петербурга. Специализация: .NET, C#, JS, AI/ML, RAG, DevOps, DB, промышленная автоматизация.  
[Telegram](https://t.me/Kotrecon) | [Email](mailto:ermakov_k@mail.ru)

---

## 🏷️ Версия

| Версия    | Дата        | Изменения                                                           |
| --------- | ----------- | ------------------------------------------------------------------- |
| **1.0.0** | Январь 2026 | Начальная реализация `Result<T>`, `IError`, ASP.NET Core интеграция |

---

## 📄 Лицензия

Этот проект распространяется под лицензией MIT - см. файл [LICENSE](LICENSE).

---
